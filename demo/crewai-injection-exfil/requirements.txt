# The live harness. weir itself needs none of this - it only reads the exported
# capture.jsonl. Pinned: every claim this demo makes about span shape is
# version-dependent, and an unpinned resolve six months out silently stops
# matching the prose.
crewai==1.15.18
openlit==1.45.0
# openlit pulls in the opentelemetry SDK + OTLP HTTP exporter. A separate
# otelcol-contrib binary (see collector.yaml) file-exports the spans; install it
# from https://github.com/open-telemetry/opentelemetry-collector-releases
