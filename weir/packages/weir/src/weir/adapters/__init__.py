"""Input adapters: foreign telemetry -> CanonicalTrace.

Outside the pure analysis core (the purity contract forbids core modules
from importing this package) and constrained by its own contract to reach
only the weir.schema seam."""
