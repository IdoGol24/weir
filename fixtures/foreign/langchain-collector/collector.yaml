receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:4318

exporters:
  file:
    path: capture.jsonl

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
